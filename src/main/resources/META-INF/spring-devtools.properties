# Exclude MapStruct-generated mapper implementations from DevTools restart
# This prevents classloader issues with generated classes
# The generated mapper classes will be loaded by the base classloader, not the restart classloader
restart.exclude.mapstruct=/.*MapperImpl\.class
restart.exclude.mapper=/com/lamngo/mealsync/application/mapper/.*MapperImpl\.class

