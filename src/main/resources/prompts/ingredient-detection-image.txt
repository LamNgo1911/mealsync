Identify all specific raw, edible ingredients in the image.
CRITICAL: Use SPECIFIC names (e.g., 'beef', 'carrots', 'salmon'). SKIP if specific type is unclear. NEVER use generic terms like 'meat' or 'vegetables'.
Include estimated quantities/prep details if visible (e.g., '200g', 'diced'). Default to quantity='1', unit=''.
Ignore non-food items.

Return JSON only:
{"ingredients": [{"name": "string", "quantity": "string", "unit": "string"}]}
If empty, return {"ingredients": []}.

