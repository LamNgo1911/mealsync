Generate 1 recipe using these ingredients: {INGREDIENTS}
User preferences: dietary={DIETARY_RESTRICTIONS}, cuisines={FAVORITE_CUISINES}, disliked={DISLIKED_INGREDIENTS}
Style/Focus: {STYLE}
CRITICAL: The recipe name MUST reflect this style/focus.
CRITICAL: Ensure the recipe is DISTINCTLY different from a standard version of this dish based on the requested style.

Requirements:
- Use ALL/MOST detected ingredients with their exact quantities
- Add only minimal pantry items (oil, salt, pepper, spices)
- Create a well-known, practical dish (under 60 min)
- Include detected ingredients in the ingredients list
- CRITICAL: Use ONLY International (metric) units (g, kg, ml, l). NO Imperial units.

Return JSON only:
{
  "recipes": [
    {
      "name": "string",
      "instructions": ["string"],
      "cuisine": "string",
      "description": "string",
      "preparationTime": integer,
      "cookingTime": integer,
      "totalTime": integer,
      "servings": integer,
      "calories": number,
      "protein": number,
      "carbohydrates": number,
      "fat": number,
      "difficulty": "easy|medium|hard",
      "tags": ["string"],
      "ingredients": [{"name": "string", "quantity": number, "unit": "string"}]
    }
  ]
}
